import React from 'react'
import styled from 'styled-components'
const LandingPage = (props) =>{

    return(
        <div>
            I am the page with the intro, score, history of winnas
        </div>
    )
};

export default LandingPage;